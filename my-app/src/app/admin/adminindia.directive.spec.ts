import { AdminindiaDirective } from './adminindia.directive';

describe('AdminindiaDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminindiaDirective();
    expect(directive).toBeTruthy();
  });
});
